<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base Theme for SetupActivity -->
    <style name="Theme.AllianceAutoLogin" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Invisible Theme for MainActivity (Zero UI, No Flash, No Preview, Autoclosing) -->
    <style name="Theme.AllianceAutoLogin.Invisible" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:windowDisablePreview">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>
</resources>